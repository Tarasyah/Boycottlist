export type Company = {
  id: string;
  name: string;
  category: string;
  involvement: string;
  subCompanies: string;
  country: string;
  logo: string; // Used as a hint for placeholder image generation
};
